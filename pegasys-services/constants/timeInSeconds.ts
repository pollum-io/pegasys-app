import { JSBI } from "@pollum-io/pegasys-sdk";

export const FIVE_MINUTES_IN_SECONDS = 300;

export const FIFTEEN_MINUTES_IN_SECONDS = 900;

export const ONE_HOUR_IN_SECONDS = 3600;

export const FOUR_HOURS_IN_SECONDS = 14400;

export const ONE_DAY_IN_SECONDS = 86400;

export const ONE_WEEK_IN_SECONDS = 604800;

export const BIG_INT_FIVE_MINUTES_IN_SECONDS = JSBI.BigInt(300);

export const BIG_INT_FIFTEEN_MINUTES_IN_SECONDS = JSBI.BigInt(900);

export const BIG_INT_ONE_HOUR_IN_SECONDS = JSBI.BigInt(3600);

export const BIG_INT_FOUR_HOURS_IN_SECONDS = JSBI.BigInt(14400);

export const BIG_INT_ONE_DAY_IN_SECONDS = JSBI.BigInt(86400);

export const BIG_INT_ONE_WEEK_IN_SECONDS = JSBI.BigInt(604800);
