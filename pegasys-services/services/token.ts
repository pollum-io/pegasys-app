import { ChainId, Currency } from "@pollum-io/pegasys-sdk";

class TokenServices {
	static async approve() {
		
	}

	static async usdcPrice(currency: Currency, chainId?: ChainId) {
        
    }
}

export default TokenServices;
